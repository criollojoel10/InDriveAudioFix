<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="InDrive Audio Fix"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!-- Metadatos (legacy) para Xposed/LSPosed -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="Fuerza AudioAttributes, AudioFocus y MediaSession en inDrive para que el audio funcione en Android Auto, incluso cuando la app está en segundo plano." />

        <!-- Mínimo de API legacy de Xposed (82) -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
    </application>

</manifest>